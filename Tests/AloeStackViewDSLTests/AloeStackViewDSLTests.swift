import XCTest
@testable import AloeStackViewDSL

final class AloeStackViewDSLTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AloeStackViewDSL().text, "Hello, World!")
    }
}
